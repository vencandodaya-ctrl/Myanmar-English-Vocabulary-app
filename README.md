# Myanmar-English Dictionary starter

Upload these files to GitHub Pages.

For reliable offline voice, create an `audio` folder and add one MP3/OGG file per word. Set that file path in `data/words.json`. Browser speechSynthesis is only a fallback and depends on the phone's installed speech engine.
